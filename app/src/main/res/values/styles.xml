<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:fontFamily">@font/open_sans</item>
        <item name="android:textStyle">bold</item>
    </style>



    <style name="ratingBar" parent="Theme.AppCompat">
        <item name="color">@color/red</item>
        <item name="colorControlNormal">@color/limeGreen</item>
        <item name="colorControlActivated">@color/red</item>
    </style>

    <style name="circleRating" parent="@android:style/Widget.RatingBar">
        <item name="android:progressDrawable">@drawable/rating_bar_symptoms</item>
        <item name="android:minHeight">32dp</item>
        <item name="android:minWidth">32dp</item>
    </style>
    <style name="MySpinnerDatePickerStyle" parent="android:Theme.Material.Light.Dialog">
        <item name="android:datePickerStyle">@style/MySpinnerDatePicker</item>

    </style>

    <style name="MySpinnerDatePicker" parent="android:Widget.Material.Light.DatePicker">
        <item name="android:datePickerMode">spinner</item>
    </style>

    <style name="seekBarHeatmap" >
        <item name="android:progressBackgroundTint">@color/white</item>
        <item name="android:progressTint">@color/white</item>
        <item name="android:colorControlActivated">@color/white</item>
    </style>

<!--    <style name="blueButton" parent="Widget.AppCompat.Button" >-->
<!--        <item name="android:background">@drawable/button_selector</item>-->
<!--    </style>-->

</resources>
